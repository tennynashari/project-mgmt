# Database Configuration
DATABASE_URL="postgresql://projectapp:password_anda_yang_kuat@localhost:5432/project_management?schema=public"

# JWT Configuration
# WAJIB DIGANTI! Generate dengan command:
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# Hasil akan berupa string random 64 karakter hexadecimal
JWT_SECRET="GANTI_DENGAN_HASIL_GENERATE_COMMAND_DI_ATAS"

# Server Configuration
PORT=4000
NODE_ENV=production

# Frontend URL (sesuaikan dengan domain Anda)
FRONTEND_URL=https://yourdomain.com

# Optional: CORS Origins (comma separated)
# CORS_ORIGINS=https://yourdomain.com,https://www.yourdomain.com
